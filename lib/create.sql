CREATE TABLE owners
(id INTEGER PRIMARY KEY,
title TEXT,
category INTEGER,
start TEXT
end TEXT);

CREATE TABLE user
(id INTEGER PRIMARY KEY,
name TEXT,
age INTEGER,
 );

 CREATE TABLE pledge
 (user_id INTEGER PRIMARY KEY,
 cat_id  TEXT,
 age INTEGER,
  );
